# Backlog

This backlog is limited to near-term documentation and design work.

Implementation work should be added later, after the documentation contract is stable enough to support it.

## Current Priorities

1. Reconcile top-level docs so `README.md`, `AGENTS.md`, and the `docs/` set use the broader workspace-tool framing consistently.
2. Add cross-links where `CLI_CONTRACT.md`, `REQUEST_RESPONSE_SCHEMA.md`, and `SECURITY_MODEL.md` share assumptions.
3. Decide whether `THREAT_MODEL.md` is needed or whether `SECURITY_MODEL.md` remains sufficient for the next phase.
4. Decide whether to add `LOGGING_SPEC.md` before implementation begins.
5. Decide whether to add `POLICY_MODEL.md` before implementation begins.
6. Decide whether to add `SESSION_MODEL.md` before implementation begins.
7. Add `docs/EXAMPLES.md` only if repeated examples start cluttering the core specs.

## Why These Are Next

- The current repo value is clarity, not implementation speed.
- Future coding-agent sessions need one source of truth per concern before code exists.
- Security, CLI behavior, schema stability, and phase planning should be settled enough to prevent conflicting implementation assumptions.

## Design Follow-Ups

1. Define whether `--cwd` is required or defaults to the current directory.
2. Define whether environment inheritance is default, restricted, or configurable.
3. Define timeout behavior for child processes and process groups.
4. Define how logs should balance traceability against secret exposure risk.
5. Define whether request IDs are generated by the CLI, broker, or caller.

## Cross-Cutting Work

1. Keep docs aligned with each other before code is added.
2. Record design decisions in `docs/DECISIONS.md`.
3. Keep task summaries aligned with `.github/pull_request_template.md`.

## Task Readiness Rule

An item is ready for implementation only when the task statement includes:

- a clear requirement;
- expected files to change;
- tests to run;
- acceptance criteria;
- a PR summary format.

## Not In Scope Yet

- Rust crate setup
- CLI implementation
- process management code
- policy enforcement code
- CI or workflow automation beyond docs
